import 'package:flutter/material.dart';

class tabWatchlist extends StatelessWidget {
  const tabWatchlist({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
      Text("tabWatchlist")
    ],);
  }
}

